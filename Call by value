#include <stdio.h>
void swap(int, int);
void main()
{
int a = 10 , b = 5 ;
printf("Before swapping a =\%d , b= %d\n",a,b);
swap (a,b);
} 
void swap lint x, int y)
{
int temp;
temp = x
x=y;
y=temp;
printf(After swapping a=%d, b=%d\n",x,y);
}
