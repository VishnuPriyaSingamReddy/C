#include<stdio.h>
void swap(int *,int *);
void main()
{
int a=10,b=5;
printf("before swapping a=%d,b=%d\n",a,b);
swap(&a,&b);
}
void swap(int *x,int *y)
{
int temp;
temp=*x;
*x=*y;
*y=temp;
printf("after swapping a=%d,b=%d",*x,*y);
}
